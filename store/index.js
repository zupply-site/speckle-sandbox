import Vuex from "vuex";

const applicationStore = () => {
  return new Vuex.Store({
    modules: {},
    state: () => ({}),
    getters: {},
    actions: {},
    mutations: {},
  });
};

export default applicationStore;
